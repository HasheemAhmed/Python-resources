# Random number

# random is the library to import rano class to get random number
# as is used to give aliases to library
import random as r

print(r.randrange(1,100))

# first parameter is start and second parameter is stop